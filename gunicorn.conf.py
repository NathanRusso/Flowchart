bind = "0.0.0.0:8000"   # Listen for request on every network interface available inside this container
workers = 4             # The number of worker process to spawn
accesslog = "-"         # Logs standard output
errorlog = "-"          # Logs standard error